import React from 'react-native';

const routesConfig = () => {
    screenOptions: {
        {
            cardStyle: {
                backgroundColor: '#f0f0f5'
            }
        }
    }
}

export default routesConfig;